#!/bin/zsh
# Credentials are now managed via ~/.okx/config.toml — no keychain sync needed.
# This script is kept as a no-op placeholder.
echo "Credentials are read directly from ~/.okx/config.toml"
echo "To update keys, edit ~/.okx/config.toml directly."
